import React from 'react';

const Loading = () => {
    return (
        <div className=''>
              <button className="btn loading">loading</button>
        </div>
    );
};

export default Loading;