import React from 'react';

const Service = ({Service}) => {
    const {name,slots}=Se
    return (
        <div>
              <h1>{Service.length}</h1>
        </div>
    );
};

export default Service;