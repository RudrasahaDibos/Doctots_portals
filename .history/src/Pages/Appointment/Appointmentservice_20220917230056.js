import React from 'react';

const Appointmentservice = ({date}) => {
    return (
        <div>
               <h4>Available Services on{date}</h4>
        </div>
    );
};

export default Appointmentservice;